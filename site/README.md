# Siege Docs Site

Vocs site for player-facing Siege documentation.

```bash
cd site
bun install
bun run dev
bun run test
bun run build
```

Docs live in `site/docs/pages`. Shared card data lives in `site/src/data`.

The pages should describe the live code, not the historical specs in `docs/superpowers`.
